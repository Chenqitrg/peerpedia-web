# M2.6: User Footprint + Compile-on-Demand — Implementation Plan

**Goal:** User profile page showing personal arXiv + compile Typst on demand instead of storing PDFs.

## Task A: User Profile Page

**Files:**
- MODIFY: `peerpedia/web/routes/pages.py` — add `/user/{user_id}` route
- MODIFY: `peerpedia/web/routes/api.py` — add `/api/v1/user/{user_id}/activity` endpoint
- NEW: `peerpedia/web/templates/user.html` — user profile template
- MODIFY: `peerpedia/web/static/style.css` — user profile styles
- NEW: `tests/test_user_profile.py` — 3 tests

**Query logic:**
- `articles WHERE founding_authors LIKE %user_id%` → 原创
- `articles WHERE mirror_by = user_id` → 搬运
- `reviews WHERE reviewer_id = user_id` → 审稿
- Merged sorted by time → 最近活动

## Task B: Compile on Demand

**Files:**
- MODIFY: `peerpedia/submit.py` — remove compile step from submit flow
- MODIFY: `peerpedia/web/routes/api.py` — add `GET /api/v1/articles/{id}/compile?fmt=html|pdf`
- MODIFY: `peerpedia/web/routes/pages.py` — article page triggers compile
- MODIFY: `peerpedia/web/templates/article.html` — add compile/render area
- MODIFY: `tests/test_submit.py` — update tests (compile removed)
